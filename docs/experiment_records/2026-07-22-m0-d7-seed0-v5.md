# M0 D7 seed0 v5 有效基线记录

| 字段 | 内容 |
| --- | --- |
| Run ID | `b0-d7-seed0-20260722-v5` |
| 运行代码 | `ebc3ee81d4e289822b07b3e7219352762d9014f2` |
| Runner SHA-256 | `003173a345c3f899e8b3034e48098dde87b9d801551f94a93724838bf0fc45d3` |
| Manifest SHA-256 | `cee24367b632a83dedbb6bd85511e8c948b1a6f8014edb497fe7a5c68c83f978` |
| 原始结果 | `results/thesis_m0/b0-d7-seed0-20260722-v5/` |
| 运行时间 | `2026-07-22T06:46:41.187368+00:00` 至 `2026-07-22T08:45:35.143289+00:00` |
| 结论 | **seed0 有效：7/7 pipeline valid，允许进入 M1 pilot 与 D7 seeds1/2** |

## 1. 路线结果

路线顺序保持为 `18、6、12、30、36、39、0`。驾驶失败状态是 Leaderboard 产生的有效模型结果，不是基础设施失败。

| Route | Driving Score | Route Completion | Infraction Score | Leaderboard status |
| ---: | ---: | ---: | ---: | --- |
| 18 | 0.817611 | 3.003681% | 0.272203 | `Failed - Agent deviated from the route` |
| 6 | 38.343744 | 67.526591% | 0.567832 | `Failed - Agent timed out` |
| 12 | 28.603825 | 53.492990% | 0.534721 | `Failed - Agent deviated from the route` |
| 30 | 12.330754 | 12.330754% | 1.000000 | `Failed - Agent deviated from the route` |
| 36 | 19.093002 | 42.169597% | 0.452767 | `Failed - Agent got blocked` |
| 39 | 37.336627 | 82.058521% | 0.455000 | `Failed - Agent timed out` |
| 0 | 1.404419 | 29.124797% | 0.048221 | `Failed - Agent got blocked` |

每条 attempt 均满足 `pipeline_valid=true`、`process_exit_code=0`、`cleanup_error=null`；总 manifest 明确记录 `recorded=7`、`planned=7`、`pipeline_valid=7`、`pipeline_invalid=0`。

## 2. 资源释放

launcher PID `1910172` 已退出。完成后复核未发现属于该 run 的 `run_thesis_baseline`、`leaderboard_evaluator` 或 CARLA 进程，TCP `2155/2255` 无监听；GPU 6/7 分别回到 `81/45 MiB`，两卡均无 active compute owner。端口释放等待均为 `0.0 s`，七个 attempt 的 GPU 释放等待为 `0.326-0.472 s`。

服务器上另有长期运行的 CARLA `world-port=2000`，不属于本 run，也未被清理。本记录只证明 v5 评测资源已释放，不证明整台服务器无其他用户作业。

## 3. 证据边界

v5 使用修复后的 runner 从七条路线完整重跑，不与 v3 的五条有效 attempt 拼接。v3/v4 原目录继续保留，分别记录 CARLA readiness 原生崩溃与 GPU compute-owner 门禁事故；它们不进入 seed0 聚合。

本记录冻结的是 B0 开发回归集 seed0 的可复现结果，不是最终 A36 论文分数。D7 seeds1/2 仍须使用相同路线顺序、GPU/端口配置与串行 runner，并分别达到 7/7 pipeline valid。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
