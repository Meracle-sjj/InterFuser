# M0 Route 18 / Seed 0 Runner Smoke

| 字段 | 内容 |
| --- | --- |
| 日期 | 2026-07-22 |
| 状态 | **ACCEPTED AS PIPELINE SMOKE** |
| 目的 | 验证 P0 → 单路线拆分 → CARLA → 完整场景 → InterFuser → Leaderboard JSON → 资源回收链路 |
| 路线 | Route 18，原始 Town04 路线语义，运行时加载 Town04_Opt |
| 随机种子 | Traffic Manager 0，CARLA Provider 2000 |
| 背景交通 | 请求 200，成功生成 200，warmup 后 165 辆运动 |

本记录只证明评测基础设施可运行，不证明当前模型具备可接受驾驶性能，也不替代 D7/A36 正式基线。

## 1. 第一次运行：无效并保留

Run ID：`b0-d7-route18-seed0-20260722`

- Git HEAD：`ec6d711519bba8b265ccb027d8086d1dd81883de`；
- runner SHA-256：`a4eee8254c487ed4d095db5fa0f358f36ca7f3a78e171163e2c4c496bf374207`；
- config SHA-256：`2276100adb8b33f4105ce65d5040c2561ded44484919bb2cf3e8732c7f40b3f8`；
- evaluator 状态：`Failed - Agent couldn't be set up`；
- 根因：runner 未向 agent 子进程注入必需的 `ROUTES` 环境变量；
- 次生缺陷：旧解析器把“结构完整 JSON + 进程码 0”误判为 pipeline valid；
- 修复提交：`e8b9345ad31d8a54540e5bd1cd756f58b6da3232`。

该 run 永久无效。原始目录保留用于证明失败与修复过程，不得进入任何模型分数汇总。

## 2. 第二次运行：有效 smoke

Run ID：`b0-d7-route18-seed0-20260722-v2`

### 2.1 来源

- Git HEAD：`e8b9345ad31d8a54540e5bd1cd756f58b6da3232`；
- runner SHA-256：`d8cf9260dd63e093eb25ad046b9f7d0ca9e39c6098be329de48e6f7057bfc2fb`；
- config SHA-256：`2276100adb8b33f4105ce65d5040c2561ded44484919bb2cf3e8732c7f40b3f8`；
- checkpoint SHA-256：`8feebbe02fab204e25ea19db01a05d7a3b6d109ab993b781da4fc2b1f4d3d8b8`；
- 场景文件：`leaderboard/data/42routes/42scenarios.json`，不是 `no_scenarios.json`。

### 2.2 运行完整性

| 指标 | 结果 |
| --- | ---: |
| Pipeline valid | true |
| Evaluator exit code | 0 |
| External timeout | false |
| Runner wall time | 200.076 s |
| Game duration | 24.400 s |
| System duration | 151.620 s |
| Route length | 141.421 m |
| Agent GPU 6 peak | 1,308 MiB |
| CARLA GPU 7 peak | 5,800 MiB |

运行结束后 2155/2255 端口空闲，runner/evaluator/CARLA 进程全部退出，GPU 6/7 回落到 81/45 MiB。CARLA shell 由 runner 发送 SIGTERM，记录退出码 `-15`；这是本次受控回收结果，不是评测中的 simulator crash。

### 2.3 驾驶结果

| 指标 | 结果 |
| --- | ---: |
| Driving Score | 1.242318 |
| Route Completion | 2.935354% |
| Infraction Score | 0.423226 |
| 状态 | Failed - Agent deviated from the route |

违规计数：环境碰撞 1、越出路线车道 1、闯红灯 1、路线偏离 1；行人碰撞、车辆碰撞、停车标志违规、route timeout 和 agent blocked 均为 0。

这是有效的模型失败：agent 完成 setup 并实际驾驶，Leaderboard 产生正数游戏时长与完整指标。不得因为状态为 failure 而从后续配对统计中删除。

### 2.4 原始产物哈希

| 文件 | SHA-256 |
| --- | --- |
| `run_manifest.json` | `4adbe273f03cf00509ca22d2848d17fdb675cbee4085409baa2ce3006aed7c23` |
| `attempt_manifest.json` | `74ec3503f5d3d97c725750b83b322de342f385ead5a1fad432d4b3bbdcd8c7dc` |
| `leaderboard_result.json` | `b4b245cd0082c00bbe012f400d9e5e5606edece41ee9768f62279d30dbfebc9e` |

远端原始根目录：`/data/shijj/interfuser_origin/results/thesis_m0/b0-d7-route18-seed0-20260722-v2/`。

## 3. 结论与下一门槛

D7 runner 的单路线真实场景链路通过。当前模型性能较弱，但这正是 B0 应保留的事实，不能通过退回无场景评测来美化。

下一门槛是 D7 单种子：route `00、06、12、18、30、36、39` 各运行 seed 0，要求 7/7 都得到 pipeline-valid 结果。根据历史时长估算需要约 4 小时独占窗口；完成前不启动 seed 1/2，也不进入 A36。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
